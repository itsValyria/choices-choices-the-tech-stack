---
import Layout from '../layouts/layout.astro';
import Searchbar from '../components/searchbar.astro'
import { fetchData } from '../lib/fetch-api-data';

// Fetching data from the Directus API
const { webinars, contourings } = await fetchData();
---

<Layout title="Oncollaboration | Home" description="Where radiotherapists all across the world meet.">

  <h1>Home</h1>
  <Searchbar></Searchbar>

  <h2>Webinars</h2>
  <!-- <ul>
    {webinars.map(webinar => (
      <li>
        {webinar.title}
        <img src={`https://fdnd-agency.directus.app/assets/${webinar.thumbnail.id}`} alt="thumbnail" />
      </li>
    ))}
  </ul> -->

  <h2>Contourings</h2>
  <!-- <ul>
    {contourings.map(contouring => (
      <li>
        {contouring.title}
        <img src={`https://fdnd-agency.directus.app/assets/${contouring.image_scan.id}`} alt="CT-Scans" />
      </li>
    ))}
  </ul> -->
</Layout>